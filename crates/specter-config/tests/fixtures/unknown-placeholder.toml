[[watch]]
name    = "build"
path    = "/"
# Lowercase non-catalog name: typo of `$path`. Uppercase names like
# `$Path` / `$SPECTER_PATH` pass through as literal for shell expansion;
# only lowercase catalog misses are typos.
command = ["fmt", "$paht"]
